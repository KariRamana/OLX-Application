package com.zensar.olxadvertise.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AdvertiseSwaggerConfi {

//	@Bean
	/*
	 * public Docket getCustomizedDocket() { return new
	 * Docket(DocumentationType.SWAGGER_2) .select()
	 * .apis(RequestHandlerSelectors.basePackage("com.zensar.olxadvertise"))
	 * .build() .apiInfo(getApiInfo()); }
	 * 
	 * 
	 * 
	 * private ApiInfo getApiInfo() {
	 * 
	 * return new ApiInfo("olx-advertise management",
	 * "This application manages olx-advertise", "1.0.0","http://www.zensar.com",
	 * "Ramana", "Apache", "https://tomcat.apache.org/"); }
	 */

}
